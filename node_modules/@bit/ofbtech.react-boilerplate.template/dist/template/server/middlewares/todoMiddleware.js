"use strict";

var Service = require('../services/todos');

module.exports = function (req, res, next) {
  try {
    Service.getToDos(function (error, response) {
      if (error) {
        next();
        return;
      }

      res.setProp('toDo', {
        todos: response
      });
      next();
    }, res.getRequestHeaders());
  } catch (error) {
    console.log('error', error);
    throw error;
  }
};

//# sourceMappingURL=todoMiddleware.js.map